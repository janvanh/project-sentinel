%% ============================================================
%%  project-config.tex
%%  PROJECT-WIDE settings, shared by every Sentinel document.
%%  Edit this file once; it applies everywhere.
%%  Document-specific settings (title, version, status...) live
%%  in each document's own doc-config.tex instead.
%% ============================================================

\SetProjectName{Project Sentinel}
\SetProjectTagline{Privacy Automation Platform}

% Default author, used unless a document overrides it in its own
% doc-config.tex with \SetDocAuthor{...}.
\SetDocAuthor{Jan van Hout}
